export interface IProduct {
  name: string;
  price: number;
  description: string;
  category: string;
  stock: number;
}

export interface ProductDTO {
  name: string;
  price: number;
  description: string;
  category: string;
  stock: number;
}
